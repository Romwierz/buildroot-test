config BR2_PACKAGE_TIM_PCI_MODULE
	bool "tim-pci-module"
	depends on BR2_LINUX_KERNEL
	help
          Module implementing the virtual timer driver

          http://www.nowhere

comment "tim-pci-module needs a Linux kernel to be built"
	depends on !BR2_LINUX_KERNEL
